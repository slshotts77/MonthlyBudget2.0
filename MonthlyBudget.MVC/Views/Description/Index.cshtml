@model IEnumerable<MonthlyBudget.Models.DescriptionListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescriptionId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescriptionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescriptionId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescriptionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DescriptionId }) |
            @Html.ActionLink("Details", "Details", new { id = item.DescriptionId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.DescriptionId })
        </td>
    </tr>
}

</table>
